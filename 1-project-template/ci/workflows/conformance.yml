name: conformance
on:
  workflow_run:
    workflows: [core-check, csharp-check]
    types: [completed]
jobs:
  summarize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Aggregate conformance to compliance/T2 & T4
        run: |
          echo "TODO: parse test results and generate T2/T4 summaries"
